coordinates = (4, 5)   # immutable; cannot be changed or modified; once you create a tuple and you can't change it.
print(coordinates)   # access element in tuple. tuple[]
print(coordinates[1])
# coordinates[0] = 10   # tuple is immutable
print(coordinates)
